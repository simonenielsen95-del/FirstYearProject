<UserControl x:Class="NEFAB.Views.PackageStatusOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NEFAB.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Label Content="List af STATUS" HorizontalAlignment="Center" Margin="0,40,0,0" VerticalAlignment="Top"/>
        <ListBox ItemsSource="{Binding OCPackageStatus}" Margin="65,208,70,132">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Status}" FontWeight="Bold" Margin="0,0,10,0" />
                        <TextBlock Text="{Binding Comment}" />
                        <TextBlock Text="{Binding EmployeeId}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Tilbage" Command="{Binding NavigateToPackageViewCommand}" HorizontalAlignment="Left" Margin="120,375,0,0" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
